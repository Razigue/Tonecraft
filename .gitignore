# Dependencies
node_modules/

# Astro / Vite build output
dist/
.astro/
.vercel/
.netlify/

# WASM build artifacts (Emscripten output is regenerated by the build)
build/
*.o
*.bc

# Env
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
Thumbs.db
*.swp

# Claude Code — machine-local permissions, not shared
.claude/settings.local.json

# BMAD — personal install answers (see _bmad/custom/.gitignore)
*.user.toml
